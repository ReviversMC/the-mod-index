name: Json Check

on:
  push:
    branches: [ main, next/*, v* ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Do the checking of index and manifest files
      uses: reviversmc/the-mod-index-validation@v2